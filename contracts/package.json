{
  "name": "emberlend-contracts",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:local": "hardhat run scripts/deploy.ts --network localHedera",
    "deploy:testnet": "hardhat run scripts/deploy.ts --network hederaTestnet"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.10",
    "typescript": "^5.5.3",
    "ts-node": "^10.9.2",
    "dotenv": "^16.4.5"
  }
}
