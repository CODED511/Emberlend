# Reown AppKit and wagmi declare conflicting peer ranges, and HashConnect
# pulls its own @walletconnect tree. npm's strict peer resolution fails on
# this combination, so install the way we do locally.
legacy-peer-deps=true
